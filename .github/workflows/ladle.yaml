name: Ladle Docs
on:
  workflow_dispatch: # This allows manual triggering
  push:
    branches:
      - main
    paths: ['.storybook/**', 'src/components/**', 'src/stories/**'] # Trigger the action only when files change in the folders defined here

# Scope permissions to only what the deploy step needs.
permissions:
  contents: write

concurrency:
  group: ladle-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install Yarn
        run: npm install -g yarn

      - name: Install dependencies 🔧
        run: yarn add

      - name: Build production bundle
        run: yarn ladle-build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: docs # The branch the action should deploy to.
          folder: docs # The folder that the build:storybook script generates files.
          clean: true # Automatically remove deleted files from the deploy branch